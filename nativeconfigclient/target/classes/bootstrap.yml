spring:
  application:
    name: configclient
  profiles:
    active: dev
    #这个是对应nativeconfigserver文件中的configclient-Dev文件
  cloud:
    config:
      uri: http://localhost:8762
      fail-fast: true